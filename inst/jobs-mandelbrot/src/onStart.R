####################################################################
# The onStart() is called before onRun().
####################################################################
onStart <- function(...) {
  # Add any code that should be run when a job is started.
}
